#include <stdlib.h>
#include <stdio.h>
int main() {
    int N = 40;
    int tab[N];
    _Bool x;
    tab[0] = 222;
    for (int i = 0; i < 1; i++) {
        printf("%d ", tab[i]);
    }
    printf("\n");
}
