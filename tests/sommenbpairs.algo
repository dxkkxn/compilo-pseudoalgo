ALGO sommepairs
ENTREE  n
# Somme des nombres pairs jusqu'a n (inclus)
DEBUT
  VAR res <- 0
  VAR i <- 0
  TQ i <= n FAIRE
     SI i % 2 = 0 ALORS
        res <- res + i
     FSI
     i <- i + 1
  FTQ
  AFFICHER res
FIN
